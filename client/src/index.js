import React, { StrictMode } from "react";
import ReactDOM from "react-dom";
import { AppComponent } from "./js/components/App"

ReactDOM.render(
    <AppComponent></AppComponent>,
    document.getElementById("app"));
