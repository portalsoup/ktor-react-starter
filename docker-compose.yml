version: '3'
services:
  server:
    build:
      context: web/
      dockerfile: Dockerfile.server
    networks:
      - server
      - client
    ports:
      - "8080:8080"
    depends_on:
      - db
  client:
    build:
      context: client/
      dockerfile: Dockerfile.client
    ports:
      - "80:80"
    volumes:
      - ./build:/usr/share/nginx/html
    networks:
      - client
  db:
    image: postgres:alpine
    ports:
      - "5432:5432"
    volumes:
      - vdata:/var/lib/postgresql
    environment:
      POSTGRES_USER: bikes
      POSTGRES_PASSWORD: tour
      POSTGRES_DB: pgdb
    restart: always
    networks:
      - server
#  pgadmin:
#    image: dpage/pgadmin4
#    environment:
#      PGADMIN_DEFAULT_EMAIL: someone@notreal.com
#      PGADMIN_DEFAULT_PASSWORD: oohsosecure
#      PGADMIN_LISTEN_PORT: 81
#    depends_on:
#      - db
volumes:
  vdata: {}
networks:
  server:
  client: